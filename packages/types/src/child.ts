export interface Child {
  id: string;
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  gender?: string;
  avatar?: string;
  parentId: string;
  notes?: string;
  createdAt: string;
  updatedAt: string;
}
